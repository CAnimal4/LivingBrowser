# Living Browser keeps data local by default and uses the mock AI provider without keys.
# Optional future provider hooks:
# LIVING_BROWSER_AI_PROVIDER=mock
# OPENAI_API_KEY=
# LIVING_BROWSER_DATA_DIR=/absolute/path/for/dev/profile
